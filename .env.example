# Copia este archivo a .env.local y coloca tu Measurement ID de Google Analytics 4
# (formato G-XXXXXXXXXX). .env.local ya está ignorado por git, así que es seguro
# guardar aquí el ID real sin subirlo al repositorio.
VITE_GA_MEASUREMENT_ID=
